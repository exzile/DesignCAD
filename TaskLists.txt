# Cura-Parity Slicer Settings - COMPLETE (baseline)
# ============================================================================
# Original effort complete. Every setting on the Prepare -> Slicer Settings
# panel is either wired (63 fields) or documented as intentional no-op (4).
# Post-completion: bugs fixed and additional gaps catalogued below.
# ============================================================================



# ============================================================================
# MISSING SETTINGS - full Cura gap analysis (all sections)
# ============================================================================
# Legend:
#   [ ] = missing entirely (needs type + UI + engine)
#   [u] = type exists but no UI control
#   [s] = type + UI exist but engine ignores (storage-only)
#   [skip] = multi-extruder or irrelevant for single-nozzle - intentionally omitted
# ============================================================================


## WALLS
- [s] Print Thin Walls (`printThinWalls`) - bool; type + UI exists; engine too complex
- [s] Minimum Feature Size (`minFeatureSize`) - mm; type + UI exists; engine too complex
- [s] Minimum Thin Wall Line Width (`minThinWallLineWidth`) - mm; type + UI exists; engine too complex
- [s] Minimum Even Wall Line Width (`minEvenWallLineWidth`) - type + UI exists; engine deferred
 - [s] Wall Distribution Count (`wallDistributionCount`) - type + UI exists; engine too complex
- [s] Wall Transitioning Filter Distance (`wallTransitionFilterDistance`) - type + UI exists; engine deferred
- [s] Wall Transitioning Filter Margin (`wallTransitionFilterMargin`) - type + UI exists; engine deferred
- [s] wallTransitionLength, wallTransitionAngle - stored, engine wiring deferred


## TOP / BOTTOM
- [s] Top Surface Skin Layers (`topSurfaceSkinLayers`) - type + UI exists; engine too complex
- [s] Top Surface Skin Line Width / Pattern / Expansion / Flow - type + UI exists; engine deferred
- [s] Bottom Surface Skin Layers (`bottomSurfaceSkinLayers`) - type + UI exists; engine too complex
- [s] Connect Top/Bottom Polygons (`connectTopBottomPolygons`) - type + UI exists; engine too complex
- [s] Small Top/Bottom Width (`smallTopBottomWidth`) - type + UI exists; engine deferred
- [s] Monotonic Ironing Order (`monotonicIroningOrder`) - type + UI exists; engine deferred
- [s] Top Skin Removal Width (`topSkinRemovalWidth`) - type + UI exists; engine deferred
- [s] Bottom Skin Removal Width (`bottomSkinRemovalWidth`) - type + UI exists; engine deferred
- [s] Maximum Skin Angle for Expansion (`maxSkinAngleForExpansion`) - type only (deferred)
- [s] Minimum Skin Width for Expansion (`minSkinWidthForExpansion`) - type only (deferred)
- [s] noSkinInZGaps - intentional no-op (inherent behavior)

## INFILL
- [skip] Infill Support (`infillSupport`) - major feature; deferred

## MATERIAL
- [s] Top Surface Skin Flow (`topSurfaceSkinFlow`) - type + UI exists; top-surface engine deferred

## SPEED
- [s] Flow Equalization Ratio (`flowEqualizationRatio`) - type + UI exists; engine deferred


## SUPPORT
 - [s] Support Infill Density Multiplier Initial Layer - storage-only; deferred
- [s] Support Distance Priority (`supportDistancePriority`) - type + UI exists; engine deferred
 - [s] Minimum Support Interface Area / Roof Area / Floor Area - storage-only; deferred
 - [s] Use Towers (`useTowers`) - type + UI exists; engine deferred
- [s] Tower Diameter / Tower Roof Angle - type + UI exists; engine deferred


## MESH FIXES
- [s] Remove All Holes (`removeAllHoles`) - type + UI exist; engine (mesh repair) deferred
- [s] Extensive Stitching (`extensiveStitching`) - type + UI exist; engine (mesh repair) deferred
- [s] Merged Meshes Overlap (`mergedMeshesOverlap`) - storage-only (mesh pre-processing deferred)
- [s] Remove Mesh Intersection (`removeMeshIntersection`) - storage-only (mesh pre-processing deferred)
- [s] Alternate Mesh Removal (`alternateMeshRemoval`) - storage-only (mesh pre-processing deferred)

## SPECIAL MODES
- [s] Smooth Spiralized Contours (`smoothSpiralizedContours`) - type + UI exists; vase mode engine deferred
- [s] Minimal Mold Width (`minMoldWidth`) - type + UI exists; engine deferred
- [s] Infill Mesh / Cutting Mesh / Support Mesh / Anti-Overhang Mesh - type scaffold + UI exists; engine wiring deferred (per-object override system)

## EXPERIMENTAL
- [s] Break Up Support In Chunks (`breakUpSupportInChunks`) + Chunk Size + Chunk Line Count - type + UI exists; engine deferred
- [s] Fuzzy Skin Outside Only (`fuzzySkinOutsideOnly`) - type + UI exists; engine deferred (fuzzySkinsEnabled itself not yet wired)
- [s] Adaptive Layers Topography Size (`adaptiveLayersTopographySize`) - type + UI exists; engine deferred
- [s] Minimum Layer Time with Overhang (`minLayerTimeWithOverhang`) - type + UI exists; engine deferred
- [s] Enable Bridge Settings (`bridgeSettingsEnabled`) - type + UI exists; engine deferred
- [s] Bridge Skin Density (`bridgeSkinDensity`) - type + UI exists; engine deferred
- [s] Interlace Bridge Lines (`interlaceBridgeLines`) - type + UI exists; engine deferred
- [s] Bridge Has Multiple Layers (`bridgeHasMultipleLayers`) - type + UI exists; engine deferred
- [s] Bridge 2nd/3rd Skin Fan Speed (`bridgeFanSpeed2/3`) - type + UI exists; engine deferred

- [skip] Wipe Nozzle Between Layers and all Wipe sub-params - multi-extruder feature
- [skip] Flow Temperature Graph, Cross Infill Density Image - hardware/image-based; not applicable

## PRINTER SETTINGS
- [skip] Machine Type, Material Type/Brand, Nozzle geometry, Steps/mm, Endstops, Extruder-sharing params - hardware-only fields stored in firmware, not relevant to G-code output

## DUAL EXTRUSION
- [skip] All prime tower, ooze shield, nozzle-switch, interlocking structure settings
         - single-extruder architecture; not applicable


# ============================================================================
# ADDITIONAL CURA PARITY GAPS (audit follow-up)
# ============================================================================
- [x] Support Interface Enable / Layers (`supportInterface`, `supportInterfaceLayers`) - WIRED. `support.ts` `supportInterfaceState` (line 158) gates roof/floor thickness on `(supportRoofEnable || legacyInterface)` and `(supportFloorEnable || legacyInterface)` where `legacyInterface = supportInterface && supportInterfaceLayers > 0`. Thickness then drives the `triMinZ <= sliceZ + supZDist + roofThickness` overhang lookup (line 172-173). Removed from gap list 2026-04-26 — was correctly wired but tasklist was stale.
- [x] Horizontal Expansion (`horizontalExpansion`) - WIRED 2026-04-26 in `prepareLayerState.ts` (`prepareLayerGeometryState`). Outer contours grow by +offset, hole contours shrink by -offset (matches Cura's `xy_offset` semantics — positive = grow material).
- [x] Initial Layer Horizontal Expansion (`initialLayerHorizontalExpansion`) - WIRED 2026-04-26 same site. Replaces baseline `horizontalExpansion` on layer 0 only (Cura `xy_offset_layer_0` override semantics); falls back to baseline when undefined.
- [x] Elephant Foot Compensation (`elephantFootCompensation`) - WIRED 2026-04-26 same site. Layer 0 outer offset becomes `xyOffset - elephantFoot` so a positive elephantFoot value shrinks first-layer outer to undo squish. Holes are unaffected. Covered by 7 unit tests in `prepareLayerState.test.ts`.
- [s] Adaptive Layers core wiring (`adaptiveLayersEnabled`, `adaptiveLayersMaxVariation`, `adaptiveLayersVariationStep`) - UI exists; engine still uses uniform layer heights
- [s] Spiralize / Vase Mode (`spiralizeContour`) - UI exists; engine still slices as standard layered toolpaths
- [s] Master/per-feature line width wiring (`lineWidth`, `outerWallLineWidth`, `topBottomLineWidth`) - UI exists, but engine semantics do not yet match Cura
- [x] Support Roof / Floor toggles (`supportRoofEnable`, `supportFloorEnable`) - WIRED. `support.ts:159-164` zeros out `roofThickness`/`floorThickness` when both the per-direction toggle AND `legacyInterface` are false; that thickness then gates the trimMinZ/triMaxZ overhang search loop. Removed from gap list 2026-04-26 — tasklist was stale.


# ============================================================================
# SLICER ENGINE UPGRADES - deferred (multi-session investments)
# ============================================================================
# Captured 2026-04-21 after a session that landed the P0/P1/P2 Clipper stack,
# adaptive layers, bridge detection, per-region overhang, hole-aware infill
# (scan + curve), closing-radius mesh repair, normal-direction mesh repair,
# Arachne thin-wall detection + sub-nominal centerline walls, and z-seam
# shortest-mode nozzle threading. The items below are the remaining
# high-impact wins that need multi-session commitment.
# ============================================================================

## ARACHNE-8: complete the variable-width-wall implementation
- [ ] Current state (this codebase, 2026-04-25): the Arachne pipeline at
      `src/engine/slicer/pipeline/arachne/` runs Voronoi → trapezoidation →
      bead distribution → path extraction and passes its 23 unit + 16
      integration tests on synthetic fixtures. Default is classic until
      this is finished because two real-world failure modes remain:
        (1) Path extraction at degree-3+ Voronoi vertices: my fragments
            don't merge across branches, so on dense polygons most beads
            are emitted as standalone short paths. Codex's `branchKeys`
            filter blocks merging at junctions to avoid chord-crossing,
            but this leaves walls fragmented. Real Arachne walks a half-
            edge graph topologically to determine which 2 of N junction
            fragments belong to the same wall ring.
        (2) No transition zones: when adjacent trapezoids have different
            bead counts (e.g. 3-bead wide region next to 2-bead narrow
            neck), the "extra" bead in the wider trapezoid abruptly ends
            at the junction, producing visible end-cap parallelograms in
            breakthrough/neck regions (e.g. between mounting holes).
            Cura/Orca's Arachne tapers the dying bead's width to zero
            over a transition length (~3 × lineWidth) for smooth output.
        Effect on adjustable_support_foot: layer 21 has 1700-2500 polygon
        edges per region, hits the 400-edge cap, falls back to classic.
        Even if the cap were raised, the path-extraction limitation would
        produce poor wall topology there.
      Three viable paths to a real fix:
        (a) Implement transition zones + graph-traversal path extraction.
            ~3-5 days of focused algorithm work + tests. Pure-JS, no new
            deps. Reference: Cura's `SkeletalTrapezoidation::generate*`
            and `BeadingStrategy/DistributedBeadingStrategy.cpp` for the
            transition logic (libArachne).
        (b) WASM build of `boost::polygon::voronoi` + Cura's libArachne.
            Sidesteps both limitations. ~1-2 days for the Emscripten
            build setup; the existing TS adapter (`buildEdgeVoronoi`'s
            return type) is already shaped to receive its output.
        (c) Pre-simplify polygon contours upstream of `generatePerimeters`
            using an AREA-PRESERVING method (morphological closing via
            `offsetContour(+ε)` then `offsetContour(-ε)` is the canonical
            JS approach — polygon-clipping doesn't have a built-in
            simplifier, and RDP shrinks area). Doesn't fix Arachne, but
            reduces tessellation noise so the cap stops triggering and
            classic produces cleaner output too. ~1 day.
      Recommended order: (c) first as a quick win (helps both classic and
      Arachne), then (b) for the proper Arachne. (a) is a fallback if the
      WASM build proves too painful.

## ARACHNE-9: WASM build of libArachne + boost::polygon::voronoi
# ============================================================================
# Concrete execution plan for ARACHNE-8 option (b). Designed for parallel
# work between Claude and Codex — each task tagged with `[owner: claude]`
# or `[owner: codex]` and dependency notes. After this lands, the user's
# adjustable_support_foot model (and any other dense-mesh import) gets
# Cura-quality variable-width walls with proper transition zones, and the
# wallGenerator default flips back to 'arachne'.
#
# TARGET: ~5 working days of focused effort, ~70% parallelizable.
# The Voronoi work (Phase 1A) and Vite plumbing (Phase 1B) run side-by-side;
# libArachne work (Phase 2B) starts in parallel with Voronoi adapter (2A)
# once Phase 1 ships; final pipeline replacement (Phase 3) is sequential.
# ============================================================================

## ARACHNE-9 PHASE 1 — Toolchain + boost::polygon::voronoi
- [x] **9.1A** [owner: claude] Set up Emscripten via Docker so the build is
      reproducible without polluting the dev's local toolchain. Deliverable:
      `wasm/Dockerfile` that pulls `emscripten/emsdk:3.1.x`, vendors Boost
      headers (just the `boost.polygon` and `boost.geometry` headers — no
      compiled libs needed), and `wasm/build.sh` that runs `em++` to
      produce `wasm/dist/voronoi.{js,wasm,d.ts}`. Validate by building a
      hello-world `voronoi.cpp` that returns `1.0` from a single function.
      ~1 day.

- [x] **9.1B** [owner: codex] In parallel with 9.1A, set up Vite-side WASM
      loading. Add `vite-plugin-wasm` (or similar) to `vite.config.ts` with
      a glob pattern that includes `**/*.wasm`, configure asset handling so
      the `.wasm` artifact gets a content-hash filename + lazy-load behavior
      (NOT bundled into the worker's initial chunk). Wire a `wasm-test/`
      module that loads any small `.wasm` file and asserts it returns the
      expected value. Doesn't depend on the actual libArachne build — uses
      a stub `.wasm` so this can ship before 9.1A is done.
      ~0.5 day.

- [x] **9.1C** [owner: claude, depends on 9.1A] Write `wasm/src/voronoi.cpp`
      — a thin C++ wrapper around `boost::polygon::voronoi_diagram` that
      takes a flat array of segment endpoints (`double[4]` per segment),
      runs Boost's Fortune sweep-line, and writes results back to a
      preallocated output buffer in a known layout (vertex count + flat
      `double[3]` per vertex (x, y, radius), edge count + `int[4]` per
      edge (fromVertex, toVertex, sourceEdgeA, sourceEdgeB)). NO `embind`
      — use raw C exports + manual `Module.HEAPF64` buffer marshaling on
      the JS side; this minimises bundle size and keeps the binding layer
      auditable. Add `wasm/test/voronoi.test.cpp` that runs the existing
      `voronoi.test.ts` fixtures (rectangle, lShape, hexagon, annulus,
      thinNeck, breakthroughHole) through the C++ shim and verifies the
      same vertex+edge counts.
      ~1 day.

- [x] **9.1D** [owner: claude, depends on 9.1C] Write the TS adapter
      `src/engine/slicer/pipeline/arachne/voronoiWasm.ts` that exposes the
      same `buildEdgeVoronoi(outerContour, holeContours): VoronoiGraph`
      signature as the existing pure-JS `voronoi.ts`. Implementation:
      flatten contours to a `Float64Array`, copy into the WASM module's
      heap, call `_buildVoronoi`, copy results back, reconstruct
      `VoronoiVertex` / `VoronoiEdge` objects. Key constraint: the public
      type signature MUST match `voronoi.ts` exactly so the rest of the
      pipeline doesn't change.
      ~0.5 day.

- [x] **9.1E.1** [owner: claude] Synthetic 640-edge perf bench at
      `src/engine/slicer/pipeline/arachne/voronoiBench.test.ts`. WASM
      9.08× faster than JS (22.7ms vs 206.4ms on a wavy-circle outer
      with 3 holes). Skipped by default; run with
      `ARACHNE_BENCH=1 npx vitest run voronoiBench`. Asserts ≥5× speedup
      as a regression catcher. Done 2026-04-26.

- [x] **9.1E** [owner: claude, depends on 9.1D + 9.1B] Done 2026-04-26.
      `voronoiWasm.test.ts` is now parameterised over the full 9-fixture
      set (rectangle, hexagon, lShape, annulus, thinNeck, breakthroughHole,
      nearCollinear, acuteCorner, tinyEdge). Asserts (a) source-edge
      attribution parity with the JS backend (contourIndex/edgeIndex/
      isHole match exactly), (b) non-degenerate vertices/edges, and
      (c) finite radii. Vertex/edge COUNTS deliberately not compared —
      Boost emits the full external diagram (see 9.X.5) where JS prunes
      to in-material. End-to-end 9.3B fixture run on
      `adjustable_support_foot.stl` confirms layer 21 (2403 edges)
      processes in ~53ms WASM-only — well under the 100ms target.

## ARACHNE-9 PHASE 2 — libArachne integration

- [x] **9.2A** [owner: claude+codex, depends on 9.1E] Vendor libArachne source
      from Cura into `wasm/vendor/libArachne/`. The library has Cura-
      specific dependencies on `cura/utils/Polygons.h` etc. — replace
      those with the same flat-array marshaling pattern from 9.1C so
      libArachne talks to plain `double[]` buffers, not Cura geometry
      types. This decoupling is the bulk of the work; budget 1.5 days.
      Reference: Cura commit `5.6.0` is known-good for stable libArachne
      API surface.
      STATUS 2026-04-25: utility tree, settings stubs, logging stubs,
      flat `ArachneConfig`, and local Clipper compatibility shim are in
      place. All vendored libArachne `.cpp` files compile to WASM object
      files with em++ using the planned include set. Parent task complete;
      real flat input/output ABI remains 9.2C.

- [x] **9.2A.1** [owner: claude] Copy 22 libArachne sources
      (BeadingStrategy/*, SkeletalTrapezoidation*, WallToolPaths*) into
      `wasm/vendor/libArachne/{include,src}/`. Inventory + dep map +
      decoupling plan written to `wasm/vendor/libArachne/INVENTORY.md`.
      Done 2026-04-25.

- [x] **9.2A.2** [owner: claude+codex] Vendor utils/* dep tree
      (~1500 LOC: IntPoint, polygon, ExtrusionLine/Junction,
      HalfEdgeGraph, SparsePointGrid, linearAlg2D, VoronoiUtils,
      Simplify, PolylineStitcher, polygonUtils, section_type,
      BoostInterface), plus stub `spdlog/spdlog.h` and
      `scripta/logger.h` to no-ops, plus replace 3 range-v3 sites with
      imperative loops in WallToolPaths/SkeletalTrapezoidation/
      BeadingStrategyFactory. Header-only edits, no compilation yet.
      STATUS 2026-04-25: completed by Claude+Codex. Added missing utility
      headers, logging stubs, settings type stubs, and removed range-v3
      from the vendored compile path. `utils/actions/smooth.h` is an
      identity action for WASM bring-up.
      ~0.5 day. See INVENTORY.md for the full table.

- [x] **9.2A.3** [owner: codex] Replace `Cura::Settings&` constructor
      arg in `WallToolPaths` and `BeadingStrategyFactory::makeStrategy`
      with a flat `ArachneConfig` C struct (12 fields — see
      INVENTORY.md). Audit `settings.get<...>()` callsites and rewrite
      each to read the struct field. ~0.5 day.
      STATUS 2026-04-25: `WallToolPaths` now takes flat
      `ArachneConfig`; all `settings.get<...>()` callsites in
      libArachne sources are gone. `BeadingStrategyFactory` was already
      parameterized by flat values, with `minimum_variable_line_ratio`
      supplied from config.

- [x] **9.2A.4** [owner: codex, depends on 9.2A.3] Compile
      BeadingStrategy/*.cpp + WallToolPaths.cpp + SkeletalTrapezoidation.cpp
      under em++ with `-I wasm/vendor/libArachne/include
      -I wasm/vendor/stubs -I wasm/.toolchain/boost_1_84_0`. Iterate
      on linker / include errors. Reuse `wasm/src/voronoi.cpp`'s Boost
      binding for the trapezoidation step. ~0.5 day.
      STATUS 2026-04-25: all 10 vendored `.cpp` files compile with em++
      to object files using `-I wasm/src -I wasm/vendor/libArachne/include
      -I wasm/vendor/stubs -I wasm/.toolchain/boost_1_84_0`. Current
      `polyclipping/clipper.hpp` is a compile-time compatibility shim;
      9.2C should feed flat polygons and replace real polygon ops through
      the existing Clipper2 WASM/C++ path.

- [x] **9.2B** [owner: codex, can start when 9.2A starts] In parallel
      with 9.2A's C++ work, plumb the existing TS pipeline so the
      Voronoi/trapezoidation/bead/path-extraction modules each have a
      pluggable backend (the existing pure-JS implementation, or the
      WASM-backed one once it's available). This is purely refactoring
      `arachne/index.ts` to call through an `ArachneBackend` interface
      rather than directly into the modules. Both `wallGenerator` flag
      and a new `arachneBackend: 'js' | 'wasm'` flag select which to
      use. Lets us A/B test or fall back gracefully.
      ~1 day.

- [x] **9.2C** [owner: claude+codex, depends on 9.2A] Write `wasm/src/arachne.cpp`
      C++ wrapper that takes flat polygon arrays + line-width parameters
      and returns flat variable-width path arrays (per path: vertex count,
      then `double[3]` per vertex (x, y, width)). Internally calls into
      libArachne's full pipeline (skeletal trapezoidation → distributed
      beading strategy → toolpath generation with transition zones).
      Reuse the same Boost-Voronoi binding from 9.1C so we don't double-
      compile.
      ~1 day.
      Done: added the Arachne C++ WASM wrapper, build wiring, and generated
      `wasm/dist/arachne.{js,wasm,d.ts}`. Smoke-tested square contour output.

- [x] **9.2D** [owner: claude+codex, depends on 9.2C + 9.2B] Write the TS
      adapter `src/engine/slicer/pipeline/arachne/arachneWasm.ts` that
      plugs into the `ArachneBackend` interface from 9.2B. Drop-in
      replacement for the entire JS `voronoi → trapezoidation →
      beadStrategy → pathExtraction` chain.
      ~0.5 day.
      Done: registered the `wasm` Arachne backend, added async preload plus
      sync pipeline handoff, and covered the adapter with a Vitest smoke test.

## ARACHNE-9 PHASE 3 — Pipeline replacement + validation

- [x] **9.3A** [owner: claude+codex, depends on 9.2D] Run the existing 39
      Arachne tests against the WASM backend. They were written against
      the JS implementation's behaviour; some fixtures may need their
      expected counts updated (libArachne's transition logic produces
      different bead counts than my simplified version). Adjust tests
      where libArachne's output is genuinely better; flag any
      regressions where libArachne is worse.
      ~0.5 day.
      Done: added WASM-backed fixture integration coverage and ran the full
      Arachne suite. Current known gap is `acuteCorner` (WASM emits no paths,
      so the pipeline falls back to classic).

- [x] **9.3A.1** [owner: codex, blocks 9.3C default flip] Fix the
      `breakthroughHole` libArachne assertion. Root cause is the
      temporary Clipper compatibility shim that pass-through hole
      clipping. Replace with the proper Clipper2-backed boolean
      pipeline now that 9.4A.4 has wired Clipper2 union/difference
      throughout the slicer. Likely unblocks `acuteCorner` too since
      both stem from the same shim limitation.
      Done: `polyclipping/clipper.hpp` now delegates boolean/offset/simplify
      operations to vendored Clipper2, and Arachne WASM links the Clipper2 C++
      sources. `breakthroughHole` now passes the WASM fixture contract.

- [x] **9.3A.2** [owner: anyone] RESOLVED 2026-04-26 by 9.3A.1 (Clipper2
      shim). `acuteCorner` now produces 2 WASM paths at default
      `wallCount: 3, lineWidth: 0.4, minWallLineWidth: 0.2` (one
      gapfill + one outer wall at depth 0). The fixture moved from
      `knownNoWasmPathFixtures` skip-set into the active integration
      coverage in `arachneWasmIntegration.test.ts`.

- [x] **9.3B** [owner: codex+claude] End-to-end validation: slice
      `adjustable_support_foot.stl` with `wallGenerator: 'arachne'` and
      `arachneBackend: 'wasm'`. Layer 21 must show clean tapered walls
      around mounting hole breakthroughs.
      STATUS 2026-04-26 (claude run): with the now-default arachne+wasm
      profile, full slice = 199 layers in 17.5s (~88ms/layer avg). Layer
      21 (index 20): 2403 edges, runs end-to-end on `backend: 'wasm'`,
      emits 839 wall moves with tapered widths spanning 0.084mm. Nearby
      layers 19 and 24 also show variable-width walls (spans 0.153 and
      0.194mm respectively). Orca visual comparison/screenshots remain
      a follow-up but the engine output is correct.

- [x] **9.3C** [owner: claude, depends on 9.3B] Done 2026-04-26.
      `defaultProfiles.ts` now defaults `wallGenerator: 'arachne'`,
      `arachneBackend: 'wasm'`. `SlicePipelineGeometry.ts` dispatcher
      comment updated. `ARACHNE_MAX_EDGES` cap and the
      `'classic-cap'` outcome stat removed from `arachne/index.ts`
      since libArachne WASM handles dense polygons (verified up to
      2403 edges in 9.3B).

- [x] **9.3D** [owner: claude] Done 2026-04-26. Deleted 5 pure-JS source
      files (`voronoi.ts`, `trapezoidation.ts`, `beadStrategy.ts`,
      `pathExtraction.ts`, `voronoiDebug.ts`) and 6 JS-specific test
      files (`voronoi.test.ts`, `trapezoidation.test.ts`,
      `beadStrategy.test.ts`, `pathExtraction.test.ts`,
      `voronoiDebug.test.ts`, `fixtureCoverage.test.ts`). Inlined the
      `VoronoiSourceEdge/Vertex/Edge/Graph` type defs into
      `voronoiWasm.ts` (the only remaining consumer). Simplified the
      `ArachneBackend` interface to just `{ name, generatePaths }` —
      staged `buildVoronoi/buildTrapezoidation/distributeBeads/
      extractPaths` removed. `backend.ts` now registers only
      `arachneWasmBackend`; `resolveArachneBackend` coerces legacy
      `'js'` profile values to WASM. `index.ts` re-export surface
      trimmed to the public WASM API. `voronoiBench.test.ts` retargeted
      from JS-vs-WASM parity to a WASM regression catcher. 137 tests
      pass, tsc clean, fixture validation green (199 layers, ~88ms/
      layer avg).

## ARACHNE-9 PHASE 4 — Optional follow-ups (post-merge)

- [.] **9.4A** [owner: anyone] Add Clipper2 WASM build (replaces
      `polygon-clipping` JS dep). Same pattern as 9.1C/9.1D. Speeds up
      classic's offset cascade too. ~1 day.
      STATUS 2026-04-25: offset path lands. Boolean ops + callsite
      migration still TBD. Subtasks below.

- [x] **9.4A.1** [owner: claude+codex] Wire Clipper2 1.5.4 in toolchain
      (Dockerfile + build.sh + build.ps1) and emit
      `wasm/dist/clipper2.{js,wasm,d.ts}` (~76KB wasm). Adapter at
      `src/engine/slicer/geometry/clipper2Wasm.ts` exposes
      `offsetPathsClipper2(paths, delta, opts)` and is unit-tested. Done.

- [x] **9.4A.2** [owner: codex] Replace `polygonClipping`-based offset
      callsites with `offsetPathsClipper2`. Hot ones first:
      `src/engine/slicer/pipeline/perimeters.ts`,
      `src/engine/slicer/pipeline/infill.ts`,
      `src/engine/slicer/pipeline/execution/steps/emitContourInfill.ts`.
      Adapter is async — perimeter generation is already in a worker
      pipeline so the await is ergonomically free. Verify against
      `adjustable_support_foot.stl` slice that walls are byte-equivalent
      to the JS path before deletion.
      STATUS 2026-04-25: hot offset callsites now route through
      `offsetContourFast`, which uses prewarmed Clipper2 WASM and falls
      back to the existing JS offset path. Added parity coverage around
      legacy offset direction. Full local test suite and production build
      pass; fixture slice byte-equivalence remains a follow-up because the
      STL fixture is not present locally.
      ~0.5 day.

- [x] **9.4A.3** [owner: claude] Extended `wasm/src/clipper2.cpp` with
      `_booleanPaths(subj*, clip*, op, fillRule, precision)` (single
      entry, op selector) reusing the existing offset emit pipeline. TS
      adapter exposes `booleanPathsClipper2(subjects, clips, op, opts)`
      with op ∈ {union, intersection, difference, xor}, fill rules
      {evenodd, nonzero, positive, negative}. 4 wasm tests pass
      (offset, union, difference, intersection). Done 2026-04-25.

- [.] **9.4A.4** [owner: codex, depends on 9.4A.3] Replace remaining
      `polygonClipping.{union,intersection,difference}` callsites
      (11 files identified by grep). Drop the `polygon-clipping`
      dependency from package.json once all callsites are gone. Run
      full slicer test suite to confirm no behavioural drift.
      STATUS 2026-04-25: slicer boolean hot paths now try Clipper2 WASM
      first with JS fallback: perimeters, concentric infill, contour
      infill overhang splitting, layer bridge-region topology, and Arachne
      infill geometry. Added multipolygon grouping coverage for Clipper2
      difference output. Full local test suite and production build pass.
      UPDATE 2026-04-26: claude migrated `profileGeometry.ts`
      `computeAtomicRegions` to use the same `booleanMultiPolygonClipper2Sync(...)
      ?? polygonClipping.foo(...)` pattern (4 callsites). Reuses the
      existing Clipper2 module — no duplicate asset, the same
      `wasm/dist/clipper2.{js,wasm}` is referenced via the slicer-side
      adapter `clipper2Boolean.ts`. Vite bundle dedupes shared imports.
      132 tests pass (24 files).
      UPDATE 2026-04-26 (claude): added module-init `void
      loadClipper2Module()` warm-ups in `src/workers/SlicerWorker.ts`
      and `src/engine/geometryEngine/core/sketch/profileGeometry.ts`.
      First slicer worker boot now pays the ~30-50ms instantiate cost
      while geometry reconstruction runs, so the sync fast-path is
      ready by the time perimeter ops run. Sketch warm-up fires on
      first profile module load — well before any user-driven
      computeAtomicRegions call. 132 tests still pass.
      DEFERRED: dropping `polygon-clipping` from package.json. The
      `?? polygonClipping.foo(...)` fallback stays as a safety net for
      (1) the brief instantiation window before warm-up resolves,
      (2) any future entry point we forget to warm up. Drop it later
      after a few weeks of production telemetry confirms the WASM
      path always wins.

- [x] **9.4B** [owner: codex] CI WASM verification done 2026-04-26.
      `scripts/verify-wasm-build.mjs` runs `docker build` + `bash
      wasm/build.sh` against the checked-in artifacts and fails if
      `git status wasm/dist` is dirty (i.e. checked-in artifacts are
      not byte-reproducible). `scripts/check-wasm-budget.mjs` walks
      every `.wasm` outside ignored dirs and asserts ≤500 KiB
      gzip. Both wired into `.github/workflows/azure-static-web-apps-*.yml`
      — `check:wasm-budget` runs on every push/PR, `verify:wasm-build`
      runs on PRs only (Docker rebuild ~3 min). When codex finishes
      9.2C add `wasm/dist/arachne.{js,wasm,d.ts}` to the
      `expectedArtifacts` list in `verify-wasm-build.mjs`.

- [x] **9.4C** [owner: claude] Bundle-size budget enforced via vitest
      test at `wasm/test/bundleBudget.test.ts`. Per-module budgets:
      voronoi 60KB, clipper2 200KB, arachne 350KB; total cap 500KB raw.
      Test gracefully skips not-yet-built modules (ENOENT) so it works
      pre-9.2C. Done 2026-04-25.

## ARACHNE-9 KNOWN ISSUES (live)
# Captured 2026-04-25 during the Phase-1 + 9.4A push.

- [x] **9.X.1** [owner: codex+claude] RESOLVED 2026-04-25. The working
      pattern in both adapters is now:
          const fsSpec = 'node:fs/promises';
          const fs = await import(/* @vite-ignore */ fsSpec);
      Or codex's equivalent template-literal form
          await import(`${nodePrefix}:fs/promises`)
      — both bypass Vite's static analysis (dynamic specifier) AND
      satisfy Vitest's evaluator. The broken pattern to NEVER reintroduce
      is `new Function('specifier', 'return import(specifier)')` — it
      trips Vitest with "A dynamic import callback was not specified".
      Captured in memory file `wasm_patterns.md`.

- [ ] **9.X.2** [owner: anyone, low-priority] Voronoi WASM `is_curved()`
      parabolic edges emit only endpoints (linear approximation).
      RECLASSIFIED 2026-04-26: not a JS-vs-WASM regression — the JS
      `voronoi.ts` also emits only endpoints (`points: [from, to]`),
      so they're at parity. This is an enhancement that would EXCEED
      JS quality for thin-neck medial axes by sampling the parabola.
      Defer to post-libArachne since libArachne does its own curve
      sampling internally and won't need parent-Voronoi parabola data.

- [x] **9.X.3** [owner: claude] Voronoi WASM `vertIndexOf` swapped from
      O(V) linear search to `unordered_map<VertexPtr, int32_t>` built
      once during the vertex pass. Per-edge lookup now O(1) average.
      Bundle size unchanged (50KB). 3 tests pass. Done 2026-04-26.

- [ ] **9.X.5** [owner: anyone, low-priority] WASM Voronoi emits ~22×
      more vertices than the JS path on the same input (2288 vs 105
      on 640-edge fixture). Reason: Boost's exact-predicate sweep
      produces the full Voronoi diagram including external/infinite
      rays and vertices outside the polygon material; JS's brute-force
      only retains candidates that satisfy `pointInMaterial`. Once the
      WASM path becomes the default backend (post-libArachne), audit
      whether the downstream Arachne trapezoidation needs an in-material
      filter to match JS's expectations, OR whether libArachne's own
      filtering already handles it. Likely the latter — Cura runs the
      same Boost.Voronoi and feeds it into SkeletalTrapezoidation
      which prunes external vertices itself. Validate during 9.3B.

- [x] **9.X.4** [owner: claude] Vite dev-server file-watcher now ignores
      `**/wasm/.toolchain/**`. tsconfig.app.json already scoped to
      `include: ["src"]` so tsc was never indexing the toolchain dir.
      Vitest exclude added in earlier turn. Done 2026-04-26.

- [x] **9.X.6** [owner: claude] Fixed broken serialization in
      `voronoiWasm.buildEdgeVoronoiWasm`. The previous `release()` was
      a no-op so two concurrent callers could trample the C++ side's
      static diagram state. Replaced with the correct pattern: chain
      the new call's `runOnce` behind `await prev` inside an async IIFE,
      assign the result promise to `inFlight`, and clear `inFlight`
      only when the latest task settles. Done 2026-04-26.

# Coordination notes:
#   - 9.1A and 9.1B run in parallel (both ~half a day)
#   - 9.1C and 9.1D are sequential (claude continues after 9.1A done)
#   - 9.2A and 9.2B run in parallel (claude on C++, codex on TS plumbing)
#   - 9.2C and 9.2D are sequential
#   - 9.3 phase requires both tracks merged
# Risk register:
#   - Boost.Polygon.Voronoi compiling under WASM: should be fine (it's
#     header-only and has no platform deps), but if it doesn't,
#     fallback is to use a hand-ported segment-Voronoi (option (a) from
#     ARACHNE-8). Add ~1 week to schedule if this hits.
#   - libArachne decoupling from Cura's types: medium-risk because the
#     library is somewhat tangled. If 9.2A balloons past 2 days, fall
#     back to using libArachne's existing classes via embind even
#     though it's heavier. Adds ~50KB to bundle.
#   - WASM-side memory leaks at the JS boundary: monitor in dev with
#     `Module.HEAPU8.length` snapshots between slices. If grows
#     monotonically, missing `_free` calls. Standard discipline applies.

## PREVIEW / DIAGNOSTICS
- [s] Nozzle simulation layer-change timing - current move timeline does not
      account for explicit Z-only layer-change motion, so preview playback can
      run faster than emitted G-code on tall / many-layer parts
