DZIGN3D — OPEN TASKS
====================
Completed tasks removed to save context. Full history: git log --oneline.

STATUS KEY:  [ ] = open   [x] = done   [~] = deferred/blocked


═══════════════════════════════════════════════════════════════
SDK CORRECTNESS AUDIT — HIGH SEVERITY
═══════════════════════════════════════════════════════════════
These are correctness bugs: either DesignCAD stores data in a way that can't round-trip
Fusion 360 semantics, or user-visible behavior differs from Fusion. Verified against SDK
headers at C:\Users\joeyp\AppData\Local\Autodesk\webdeploy\production\ca305acf3852cfce8e837ee5435adf649bc398ca\CPP\include\Fusion\.

[x] CORR-1. Linear dimensions missing orientation type. Fixed: DimensionOrientation type
            added to SketchDimension, SketchDimensionPanel has orientation select, and
            SketchDimensionAnnotations.tsx now passes dim.orientation to computeLinearDimension.

[x] CORR-2. Extrude missing Two-Sides asymmetric extent. Fixed: 'two-sides' direction added
            to ExtrudePanel with separate Side 1 / Side 2 distance inputs.

[ ] CORR-3. Fillet missing Variable-Radius and Chord-Length types as proper edge-sets.
            SDK `FilletFeatureInput.h` distinguishes ConstantRadiusEdgeSet,
            VariableRadiusEdgeSet, ChordLengthEdgeSet. DesignCAD's fillet dialog
            has fields for startRadius/endRadius but treats them as a uniform
            variable across all selected edges; each edge can't have its own radius
            function. FIX: change fillet params to accept an array of edge-sets, each
            with its own type. (hard — affects geometry engine)

[ ] CORR-4. Component vs Occurrence distinction missing. SDK `Component.h` (definition)
            and `Occurrence.h` (placed instance) are separate entities — one Component
            can have many Occurrences at different transforms. DesignCAD's
            `Component` type (src/types/cad.ts) is used for both. This means:
            (a) inserting the same component twice copies all bodies instead of
                instancing them.
            (b) changes to an instance can't propagate back to the definition.
            FIX: split into ComponentDefinition + ComponentOccurrence types; store
            transform on the occurrence. (hard — assembly-wide refactor)

[ ] CORR-5. `grounded` flag on wrong entity. SDK `Occurrence.isGrounded()` —
            grounding is per-instance. DesignCAD's `component.grounded`
            (cad.ts ~line 362) is on the definition, so grounding one instance
            incorrectly grounds all copies. Fix is blocked by CORR-4.


═══════════════════════════════════════════════════════════════
SDK CORRECTNESS AUDIT — MEDIUM SEVERITY
═══════════════════════════════════════════════════════════════

[ ] CORR-6. Sketch display flags missing. SDK `Sketch.h` has
            `arePointsShown`, `areProfilesShown`, `areDimensionsShown`,
            `areConstraintsShown` as per-sketch properties. DesignCAD has
            GLOBAL toggles in SketchPalette instead — every sketch shares the
            same display settings. FIX: move these flags to the Sketch type so
            settings persist per sketch.

[x] CORR-7. `Sketch.isComputeDeferred` missing. Fixed: `sketchComputeDeferred` flag
            added to cadStore; auto-solve calls in addSketchConstraint/addSketchDimension
            check the flag; "Defer Solve" checkbox added to SketchPalette.

[ ] CORR-8. Extrude `StartExtent` variants incomplete. SDK supports
            `ProfilePlaneStartDefinition`, `OffsetStartDefinition`,
            `EntityStartDefinition` (start from a face/plane/sketch point).
            DesignCAD supports only profile+offset; can't start from another
            entity.

[ ] CORR-9. Extrude `thinExtrudeWallLocation` enum. SDK values are
            `Side1ThinWallLocation`, `Side2ThinWallLocation`,
            `CenteredThinWallLocation` (which we just aligned). BUT SDK allows
            different locations for side1 and side2 independently; DesignCAD
            has a single `thinSide` value covering both. FIX: split into
            thinSideOne / thinSideTwo when two-sided extrude is added (CORR-2).

[ ] CORR-10. Revolve `isProjectAxis` missing. SDK flag controls whether the
             rotation axis is projected to the profile plane or used at its
             original 3D location. DesignCAD always uses the axis as-is.
             Can produce wrong revolve center when axis is skew to the sketch.

[x] CORR-11. Fillet `isG2` (curvature continuity) not exposed. Fixed: isG2 checkbox
             added to FilletDialog; FilletParams.isG2 typed and passed through.

[ ] CORR-12. Fillet `isRollingBallCorner` missing. SDK flag for corner
             setback solution method. Small niche but changes how fillets
             meet at vertices.

[x] CORR-13. Hole `tipAngle` default not 118°. Fixed: drillAngle defaults to 118 in
             HoleDialog, clamped to [60, 150] on change.

[ ] CORR-14. Hole `participantBodies` missing. SDK lets the hole feature
             declare which bodies the cut applies to; others pass through.
             DesignCAD cuts all visible bodies. FIX: add an optional
             participantBodies selector; default keeps current behaviour.

[x] CORR-15. Component missing origin entities. Fixed: OriginFolder sub-component
             in ComponentNode.tsx now renders collapsible tree with origin point,
             X/Y/Z axes (color-coded), and XY/XZ/YZ planes.

[ ] CORR-16. Construction collections not exposed on Component. SDK Component
             has `constructionPlanes`, `constructionAxes`, `constructionPoints`
             as first-class collections. DesignCAD collapses these into one
             `constructionIds` array — can't query "all construction points in
             this component" cleanly. Cosmetic in UI, but limits future
             reference-selector work.

[ ] CORR-17. Timeline nested groups missing. SDK `TimelineGroup` inherits
             from `TimelineObject`, so groups can contain other groups.
             DesignCAD's `FeatureGroup` is flat — a group can only hold
             features, not other groups. FIX: allow groupId on FeatureGroup,
             render nested indentation in Timeline.tsx.

[ ] CORR-18. No first-class MeshBody type. SDK `MeshBody.h` is a peer of
             `BRepBody` with its own name/visibility/material/appearance.
             DesignCAD treats mesh as a `BodyKind` string on Body; mesh bodies
             can't carry mesh-specific properties (triangle count, repair
             state, etc.) cleanly. FIX: add MeshBody type or mesh-specific
             fields on Body.


═══════════════════════════════════════════════════════════════
MESH WORKSPACE — MISSING FEATURES
═══════════════════════════════════════════════════════════════
Fusion 360 has a full Mesh workspace (Prepare + Sculpt). DesignCAD currently only
imports meshes and has the tessellate feature.

[ ] MESH-1. Mesh section plane / mesh slice preview — view mesh cross sections.
[ ] MESH-2. Reduce Mesh — triangle decimation with target count or percentage.
[ ] MESH-3. Remesh — retopologize a mesh to uniform triangles.
[ ] MESH-4. Smooth Mesh — Laplacian or Taubin smoothing.
[ ] MESH-5. Make Closed Mesh — hole filling / watertight repair.
[ ] MESH-6. Erase and Fill — delete triangles and rebuild.
[ ] MESH-7. Mesh boolean (union/subtract/intersect between mesh bodies).
[ ] MESH-8. Convert Mesh to BRep — for simple meshes, retopologize to faceted BRep.
[ ] MESH-9. Separate Mesh — split disjoint mesh islands into individual bodies.
[ ] MESH-10. Align Mesh — 3-point alignment between mesh and BRep / mesh-mesh.


═══════════════════════════════════════════════════════════════
TIMELINE — BEHAVIOR GAPS
═══════════════════════════════════════════════════════════════
[x] TL-1. Timeline navigation buttons — Previous / Next / Beginning / End controls
          added using rollbackIndex; chevron nav buttons in timeline header.
[ ] TL-2. Timeline play/record — animate feature creation chronologically.
          (Fusion: `Timeline.playFromBeginning()`.) Low priority but useful
          for design reviews.
[ ] TL-3. Nested timeline groups (see CORR-17).


═══════════════════════════════════════════════════════════════
CONTEXT MENUS — COMPLETED ✓
═══════════════════════════════════════════════════════════════
[x] CTX-1. Viewport right-click context menu — Undo/Redo, Select, Measure, New Sketch,
           Show All Bodies, Isolate, Look At. Sketch mode shows Finish Sketch.
[x] CTX-2. Feature timeline context menu — Edit, Rename, Suppress/Unsuppress,
           Roll Back To Here, Group Selected, Delete.
[x] CTX-3. Body context menu — Isolate, Show All Bodies wired up (was coming-soon stubs).
[x] CTX-4. Sketch context menu — Look At (orients camera to sketch plane normal).
           alert() calls replaced with setStatusMessage throughout.
[x] CTX-5. Added renameFeature() action to cadStore.
[x] CTX-6. Added isolateBody() / showAllBodies() actions to componentStore.
[x] CTX-7. Body context menu — Opacity Control: inline slider in context menu, expands
           in-place; Body.opacity field; ExtrudedBodies applies display opacity multiplier.
[x] CTX-8. Body context menu — Save As Mesh: MeshExporter R3F component watches store
           trigger, exports to STL (sync) or GLB (async) via three.js exporters.
[x] CTX-9. Body context menu — Selectable/Unselectable toggle: dynamic label shows
           current state; ExtrudedBodies sets userData.pickable = false when unselectable.
[x] CTX-10. Sketch context menu — Show/Hide wired to toggleFeatureVisibility on the
            sketch's linked feature; label/icon dynamically reflect current state.
[x] CTX-11. Sketch context menu — Hide Profile / Hide Projected Geometries / Hide
            Construction Geometries now call global store setters (not stubs).
[x] CTX-12. Feature timeline context menu — Move to Group: submenu lists existing groups;
            moveFeatureToGroup action added to cadStore; Remove from Group option shown
            when feature is already grouped.
[ ] CTX-13. Viewport context menu — Look At Face (pick face in viewport and orient camera).

═══════════════════════════════════════════════════════════════
SDK AUDIT — FEATURE PARAMETER GAPS
═══════════════════════════════════════════════════════════════
[ ] SDK-1. Hole — edge-offset positioning (setPositionByPlaneAndOffsets, setPositionOnEdge).
           Currently only supports face-pick placement; can't offset from reference edges.

[ ] SDK-2. Hole — To Face termination (setToFaceExtent). Currently only blind / through-all /
           to-object options.

[ ] SDK-3. Sweep — guide rail support (SweepFeatureInput.guideRail). Currently single-path
           only; add optional second rail for 3D guided sweeps.

[ ] SDK-4. Sweep — orientation type (SweepOrientationTypes: perpendicular / parallel / default)
           and profile scaling options (SweepProfileScalingOptions). Both missing from SweepPanel.

[ ] SDK-5. Sweep — path parametric positioning (distanceOne / distanceTwo — where along the
           path the profile starts/ends). Not exposed in SweepPanel.

[ ] SDK-6. Loft — rail selection (LoftFeatureInput.centerLineOrRails). Currently only cross-
           section profiles; rails and centerline guide not supported.

[ ] SDK-7. Loft — edge alignment (LoftEdgeAlignments enum: Connected / Tangent / Curvature)
           for start/end sections. Needed for G1/G2 loft tangency matching.

[ ] SDK-8. Loft — closed loft (isClosed) and tangent edge merging (isTangentEdgesMerged).
           Both exposed in SDK but absent from LoftPanel.

[ ] SDK-9. Fillet — setback distance per edge and Face Setback mode (merge adjacent faces).
           Currently only uniform radius supported.

[ ] SDK-10. Construction geometry — most plane/axis definition variants missing:
            Planes: Offset, OffsetThroughPoint, AtAngle, Tangent at Point, Midplane,
                    DistanceOnPath, TwoEdges, ThreePoints (10+ missing types).
            Axes: NormalToFaceAtPoint (missing dedicated tool).
            Currently only ~3 plane tools and ~5 axis tools implemented vs 13/11 in SDK.

[x] SDK-11. Extrude — two-sided/asymmetric extent. Fixed via CORR-2: 'two-sides' direction
            in ExtrudePanel with separate Side 1 / Side 2 distance inputs.

[ ] SDK-12. Extrude — confined faces mode (limit extrude to stay within bounding faces).

═══════════════════════════════════════════════════════════════
SOLID AUDIT — BUG FIXES
═══════════════════════════════════════════════════════════════
[x] SOL-B1. Emboss/Deboss type mismatch — Fixed: 'emboss' already in FeatureType union;
            EmbossDialog stores type 'emboss'; Timeline icon case added.

[x] SOL-B2. Pipe type mismatch — Fixed: 'pipe' already in FeatureType union;
            PipeDialog stores type 'pipe'; Timeline icon case added.

[x] SOL-B3. Boundary Fill type mismatch — Fixed: 'boundary-fill' already in FeatureType
            union; BoundaryFillDialog stores type 'boundary-fill'; Timeline icon added.


═══════════════════════════════════════════════════════════════
SOLID AUDIT — INCOMPLETE IMPLEMENTATIONS
═══════════════════════════════════════════════════════════════
[x] SOL-I1. Fillet — "Full Round" mode added (FullRoundFilletEdgeSet). FilletMode type
            ('constant' | 'variable' | 'full-round'); mode select replaces variable checkbox.

[x] SOL-I2. Shell — face selection UI wired up via ShellFacePicker R3F component;
            ShellDialog shows face chips with per-face remove buttons.

[x] SOL-I3. Draft — Parting Line mode face picker wired via DraftPartingLinePicker R3F
            component; DraftDialog shows face chip and uses face normal as pull axis.

[x] SOL-I4. Hole — ISO/ANSI/NPT size preset lookup integrated via HoleSizePresets.ts;
            Standard + Size dropdowns in HoleDialog auto-populate diameter and pitch.

[x] SOL-I5. Remove Face — general face picker wired via RemoveFacePicker R3F component;
            RemoveFaceDialog shows face chip; passthrough overlay for viewport picking.

[x] SOL-I6. Chamfer — "Three Face" mode added (ThreeEdgeChamferEdge); ChamferMode union
            extended; dialog hides distance inputs and shows hint in three-face mode.

[x] SOL-I7. Shell — "Individual Face Offsets" mode added; shellFaceThicknesses store state;
            FaceRow sub-component shows per-face thickness input when mode is active.

[x] SOL-I8. Rib/Web — "Fill All Enclosed Faces" and "Preserve Corners" toggles added to
            RibDialog; both stored in feature params.

[x] SOL-I9. Thread — ThreadSizePresets.ts with ISO/ANSI/NPT tables; ThreadDialog auto-
            populates diameter, pitch, class on standard/designation change; pitch validation.


═══════════════════════════════════════════════════════════════
SOLID AUDIT — MISSING FEATURES
═══════════════════════════════════════════════════════════════
[x] SOL-M1. Rectangular Pattern (3D) — RectangularPatternDialog added with dual-direction
            axis presets and count/spacing grid; wired in App.tsx, Toolbar, RibbonManageTab.

[x] SOL-M2. Circular Pattern (3D) — CircularPatternDialog exists; wired in App.tsx and
            RibbonManageTab toolbar.

[x] SOL-M3. Mirror Feature (3D) — MirrorDialog exists; wired in App.tsx and RibbonManageTab.

[x] SOL-M4. Move/Copy Body — MoveBodyDialog exists; wired in App.tsx and RibbonSolidTab.

[ ] SOL-M5. Coil — helix/coil generator (CoilFeatureInput in SDK). Parameters: pitch, height,
            coil type (pitch-height / pitch-revolutions / height-revolutions / spiral),
            coil section (circle / square / triangle / custom profile). (hard)

[x] SOL-M6. Thicken — ThickenDialog exists; wired in App.tsx and RibbonSurfaceTab.

[ ] SOL-M7. Stitch — merge surface bodies into a single quilt or closed solid. (medium —
            deferred until surface workflow is complete)

[ ] SOL-M8. Unstitch — split a stitched surface back into individual faces. (medium — deferred)

[ ] SOL-M9. Extend Surface — extend surface edges by distance or to another body. (medium)

[x] SOL-M10. Reverse Normal — exists; wired in toolbar.

[ ] SOL-M11. Path Pattern — pattern features/bodies along a sketch path. (hard)


═══════════════════════════════════════════════════════════════
SOLID AUDIT — TOOLBAR GAPS
═══════════════════════════════════════════════════════════════
[x] SOL-T1. Added ribbon buttons for: Draft, Rib, Thread, Emboss (RibbonSolidTab CREATE);
            Offset Face, Move Body, Mirror, Pattern (RibbonSolidTab MODIFY).


═══════════════════════════════════════════════════════════════
SKETCH AUDIT — GAPS
═══════════════════════════════════════════════════════════════
[ ] SK-A1. 3D Sketch Constraints — add surface-based constraints missing from 2D solver:
           Coincident to Surface, Perpendicular to Surface, Line on Planar Surface,
           Distance Between Point/Line and Surface. Required for real 3D sketch parity. (hard)

[ ] SK-A2. Pattern on Path — sketch pattern along an arbitrary sketch curve (Fusion:
           SketchPathPattern). Parameters: entities, path curve, count, spacing type. (medium)

[x] SK-A3. Reference Dimensions — "driven" toggle in SketchDimensionPanel; dimensionDrivenMode
           store state; new dimensions created with driven flag when checked.

[ ] SK-A4. Over-Constraint / Redundant Constraint Visual Feedback — highlight conflicting
           constraints red in SketchConstraintOverlay; show status badge in sketch palette
           when over-constrained. (medium)

[x] SK-A5. Multi-line Sketch Text — textarea with rows=3; lineCount display; uses
           sketch-palette-row--col layout; sketch-palette-textarea CSS class.

[x] SK-A6. Text Formatting — font family select (default/sans/serif/mono/cursive) and
           Bold/Italic toggle buttons in SketchTextPanel; fontPathToSegments applies
           italic shear transform; bold/italic stored and wired to SketchInteraction.

[x] SK-A7. Show/Hide Construction Geometry toggle — "Construction Geom." checkbox in
           SketchPalette; showConstructionGeometries store state.

[x] SK-A8. Dimension Tolerance — tolerance mode select (None/Symmetric/Deviation) in
           SketchDimensionPanel; toleranceUpper/toleranceLower stored on SketchDimension;
           wired into SketchInteraction for all dimension placement calls.

[ ] SK-A9. Offset Constraint — parametric offset between two parallel edges/curves as a
           geometric constraint (separate from the offset-curves tool). (medium — deferred)


═══════════════════════════════════════════════════════════════
SKETCH SECOND AUDIT GAPS
═══════════════════════════════════════════════════════════════
[ ] S4.  Isoparametric Curve — UV-parameter curve on a NURBS surface (deferred)


── INSERT ────────────────────────────────────────────────────
[ ] D194. Insert Fastener — fastener library browser to insert bolts/screws as solid bodies
          (very hard — deferred)
[ ] D195. Derive — pull bodies/sketches/params from another file with live parametric link
          (very hard — deferred)


── ASSEMBLY — MISSING TOOLS ──────────────────────────────────

[ ] A24. Constrain Components — Fusion: Assemble > Constrain Components — Inventor-style
         mate/flush/angle/tangent constraints as an alternative positioning system to Joints.
         Currently comingSoon stub. Requires a constraint solver for rigid body placement.
         (hard — deferred)

[ ] A28. Make Independent — Fusion: right-click a linked/referenced component > Make
         Independent — breaks the live reference to an external design file so the component
         becomes a local copy. Related to D195 Derive (deferred) and A13 Insert Component.
         isLinked:true flag exists on Component type but no Make Independent UI or action.
         (medium — blocked until external reference loading is real)
