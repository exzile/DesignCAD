


# ============================================================================
# MISSING SETTINGS - full Cura gap analysis (all sections)
# ============================================================================
# Legend:
#   [ ] = missing entirely (needs type + UI + engine)
#   [u] = type exists but no UI control
#   [s] = type + UI exist but engine ignores (storage-only)
#   [skip] = multi-extruder or irrelevant for single-nozzle - intentionally omitted
# ============================================================================


## TOP / BOTTOM
- [s] Maximum Skin Angle for Expansion (`maxSkinAngleForExpansion`) - type only (deferred — needs surrounding-overhang slope detection)
- [s] noSkinInZGaps - intentional no-op (inherent behavior)

## INFILL
- [skip] Infill Support (`infillSupport`) - major feature; deferred


## MESH FIXES
- [s] Extensive Stitching (`extensiveStitching`) - type + UI exist; engine (mesh repair) deferred — non-trivial to handle non-coplanar gap closure
- [s] Merged Meshes Overlap (`mergedMeshesOverlap`) - storage-only (multi-mesh pre-processing deferred — needs per-object framework)
- [s] Remove Mesh Intersection (`removeMeshIntersection`) - storage-only (multi-mesh boolean deferred)
- [s] Alternate Mesh Removal (`alternateMeshRemoval`) - storage-only (multi-mesh boolean deferred)

## SPECIAL MODES
- [s] Smooth Spiralized Contours (`smoothSpiralizedContours`) - storage-only; vase-mode Z-spiral is now wired (extrudeTo Z support, perimeter-ramped Z) but smoothing across consecutive Z increments is a separate Cura feature still pending
# Minimal Mold Width (`minMoldWidth`) + Mold Enabled (`moldEnabled`) — wired in prepareLayerState.ts as a per-layer contour transform; see audit note above.
- [s] Infill Mesh / Cutting Mesh / Support Mesh / Anti-Overhang Mesh - type scaffold + UI exists; engine wiring deferred (per-object override system — architectural)

## EXPERIMENTAL
# (fuzzy-skin family wired in 2026-04-28 sweep above; no remaining experimental gaps)

- [skip] Wipe Nozzle Between Layers and all Wipe sub-params - multi-extruder feature
- [skip] Flow Temperature Graph, Cross Infill Density Image - hardware/image-based; not applicable

## PRINTER SETTINGS
- [skip] Machine Type, Material Type/Brand, Nozzle geometry, Steps/mm, Endstops, Extruder-sharing params - hardware-only fields stored in firmware, not relevant to G-code output

## DUAL EXTRUSION
- [skip] All prime tower, ooze shield, nozzle-switch, interlocking structure settings
         - single-extruder architecture; not applicable


# ============================================================================
# ADDITIONAL CURA PARITY GAPS (audit follow-up)
# ============================================================================
# (none currently — both prior items wired in 2026-04-27 sweep above)
#
# Remaining multi-session items, in rough priority order:
#   1. Per-object mesh system (Infill / Cutting / Support / Anti-Overhang
#      Mesh) — needs per-object override resolution (per-object > profile
#      > default), boolean operations on object pairs, and override pass-
#      through into the slice run. Architectural work.
#   2. Multi-mesh repair (extensiveStitching, mergedMeshesOverlap,
#      removeMeshIntersection, alternateMeshRemoval) — extends beyond the
#      single-mesh removeAllHoles already wired. Needs the per-object
#      framework above for the multi-mesh booleans.


